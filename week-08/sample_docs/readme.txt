Sample Document Set for the RAG App

This prototype uses simple text documents and a Chroma vector store.

Each document is indexed as an embedding vector. When a user asks a question, the app retrieves the most relevant passages from these documents and uses them to answer the query.

Source documents are intentionally short so the app can respond quickly and reliably.
